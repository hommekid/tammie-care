# 🗓️ แผน: Health Timeline — ประวัติสุขภาพตามเวลา

> **สถานะ: ✅ ทำแล้ว (13 ส.ค. 2026)** — `app/timeline.html` · เข้าจากปุ่ม "📄 รายงาน" หัวหน้า pet · ดูสรุปที่ `NOTES_gotchas.md` กับดักข้อ 15
> (วางแผน 13 ส.ค. 2026) · branch `v2-multiuser`
> อ่านคู่กับ `NOTES_gotchas.md` (กับดัก) และ `FEATURES.md` (ภาพรวมฟีเจอร์)

---

## 1. เป้าหมาย

หน้าใหม่ที่รวม "ทุกอย่างที่มีวันที่" ของน้องมาเรียงเป็นเส้นเวลาเดียว — ให้เห็นภาพว่าช่วงที่ผ่านมาน้องเป็น/ทำอะไรบ้าง เลือกช่วงเวลาได้ ดูในเว็บหรือบันทึกเป็น PDF

รวมจาก 4 แหล่ง: **น้ำหนัก · อาการประจำวัน · การรักษา · ผลเลือด**

---

## 2. การตัดสินใจที่เคาะแล้ว (13 ส.ค. 2026)

| เรื่อง | เคาะเป็น | หมายเหตุ |
|---|---|---|
| วางไว้ไหน | **ไฟล์แยก `timeline.html?id=<pet>`** | ไม่ใช่แท็บที่ 7 · หน้า pet.html ใหญ่อยู่แล้ว + มี URL ของตัวเองเหมาะกับพิมพ์ PDF |
| ทำ PDF ยังไง | **print ของเบราว์เซอร์ + CSS `@media print`** | ฟรี ไม่ต้องมี library · ฟอนต์ไทย (Sarabun) เรนเดอร์ตรง ไม่เพี้ยน |
| เรียงวันที่ | **ใหม่ → เก่า** | ดูล่าสุดก่อน |
| ผลเลือดในตาราง | **ไม่โชว์ค่าผลเลือด** — บอกแค่ว่าวันนั้นมีผลเลือดหมวดอะไร | อยากดูค่าจริงให้ไปแท็บผลเลือด · กันตารางล้น |
| นัดหมาย | **ไม่รวม** | ถ้าไปนัด = ไป รพ. ข้อมูลอยู่ในหน้าการรักษาอยู่แล้ว |
| ช่วงเวลา | ปุ่ม **7 วัน / 30 วัน / 1 ปี / กำหนดเอง (ปฏิทิน)** | default เปิดมาที่ **30 วัน** (ปรับได้ตอนทำ) |

---

## 3. โครงหน้า (บนลงล่าง)

### 3.1 แถบภาพรวม (หัวหน้า)
- ชื่อน้อง (+ ชื่อเล่น) · พันธุ์ · อายุ (ใช้ `TC.ageText` เดิม) · รูป/emoji
- **น้ำหนัก "จาก X kg → Y kg"** = น้ำหนักตัวแรกในช่วง → ตัวล่าสุดในช่วง + ส่วนต่าง (▲/▼ kg) · ถ้าในช่วงมีจุดเดียวโชว์ค่าเดียว · ไม่มีเลย = ไม่โชว์บรรทัดนี้
- ช่วงวันที่ที่กำลังดู (เช่น "1 – 30 ส.ค. 2568")

### 3.2 ตัวเลือกช่วงเวลา
- ปุ่ม preset: `7 วัน` · `30 วัน` · `1 ปี` · `กำหนดเอง`
- กด "กำหนดเอง" → โผล่ช่องปฏิทิน 2 ช่อง (วันเริ่ม – วันสิ้นสุด, `input[type=date]`)
- ปุ่ม preset = นับถอยจากวันนี้ (เช่น 30 วัน = วันนี้ย้อนหลัง 30 วัน)

### 3.3 ตารางไทม์ไลน์
- ทุกเหตุการณ์ในช่วง เรียงวันที่ใหม่→เก่า
- คอลัมน์: **วันที่ · ประเภท (ป้ายสี) · รายละเอียด**
- ประเภท + สี:
  - ⚖️ **น้ำหนัก** — "8.2 kg" (+ หมายเหตุถ้ามี)
  - 🩹 **อาการ** — ป้ายสีตามหมวด (ใช้ `SYM_COLOR_MAP`/`dailyCats`) + ชื่อหมวด + รายละเอียด · มีรูปบอก "📷 n รูป"
  - 🏥 **การรักษา** — หมอ/แผนก + บันทึก (แต่ละ entry ในวันนั้นเป็น 1 แถว)
  - 🧪 **ผลเลือด** — "มีผลเลือด: CBC, เคมี" (ชื่อหมวดที่ตรวจวันนั้น) — **ไม่โชว์ค่า** · ลิงก์/ข้อความชวนไปดูที่แท็บผลเลือด
- ว่างในช่วงที่เลือก → "ไม่มีบันทึกในช่วงนี้"

---

## 4. โมเดลข้อมูล (รวม 4 แหล่งเป็นลิสต์เดียว)

normalize ทุกอย่างเป็น `{ date, type, color, title, detail, meta }` แล้ว `sort` วันที่ desc

| type | ดึงจาก | title | detail |
|---|---|---|---|
| `weight` | `vitals.weight[]` | `⚖️ น้ำหนัก` | `8.2 kg` + note |
| `symptom` | `symptoms[cat][]` (ทุกหมวด) | ชื่อหมวด (+สี) | `detail` (+ 📷 n) |
| `treat` | `treatments[].entries[]` | `🏥 การรักษา` | `doctor · specialty — notes` |
| `lab` | `labs[]` จับกลุ่มตาม `date` | `🧪 ผลเลือด` | รายชื่อหมวด (map `panel`→ชื่อจาก `labPanels`) |

หมายเหตุ:
- **ผลเลือดวันเดียวกันหลายหมวด → รวมเป็นแถวเดียว** ("มีผลเลือด: CBC, เคมี") ไม่แตกหลายแถว
- การรักษา 1 วันมีหลาย entry → แตกเป็นหลายแถวได้ (แต่ละครั้ง)
- ใช้ `parseLabVal` เฉพาะถ้าต้องแตะตัวเลข (รอบนี้ไม่ต้อง เพราะไม่โชว์ค่า)

---

## 5. PDF ด้วย print เบราว์เซอร์

- ปุ่ม **"📄 บันทึก PDF"** → `window.print()`
- `@media print` CSS:
  - ซ่อนปุ่มช่วงเวลา / ปุ่ม PDF / แถบ nav — เหลือแค่ภาพรวม + ตาราง
  - ใส่หัวกระดาษ: ชื่อน้อง + ช่วงวันที่ + "ออกเมื่อ <วันที่>"
  - `table { break-inside: auto }` · `tr { break-inside: avoid }` กันแถวถูกตัดกลาง
  - สีป้ายพิมพ์ออก (`-webkit-print-color-adjust: exact`)
- ผู้ใช้กด print แล้วเลือก "Save as PDF" ได้ทั้ง iOS/Android/desktop

---

## 6. ทางเข้า

- ปุ่ม **"🗓️ ไทม์ไลน์"** ที่หัวหน้า `pet.html` (แถวเดียวกับ ✏️ โปรไฟล์) → เปิด `timeline.html?id=<pet>`
- timeline.html มีปุ่ม ← กลับไป pet.html

---

## 7. ลำดับลงมือ

| # | งาน | ผลลัพธ์ |
|---|---|---|
| 1 | `app/timeline.html` โครงหน้า + โหลด pet (`TC.getPet`) + auth guard | เปิดหน้าได้ เห็นภาพรวมหัว |
| 2 | ฟังก์ชันรวม/ normalize 4 แหล่ง → ลิสต์เดียว เรียง desc | ได้ event list |
| 3 | ตัวเลือกช่วง (preset + custom) + กรองตามช่วง | เปลี่ยนช่วงแล้วตารางอัปเดต |
| 4 | ตารางไทม์ไลน์ + ป้ายสี + น้ำหนัก จาก→ถึง | หน้าใช้งานได้ครบ |
| 5 | `@media print` + ปุ่มบันทึก PDF | พิมพ์/เซฟ PDF สวย |
| 6 | ปุ่ม "🗓️ ไทม์ไลน์" ในหน้า pet + bump `?v=` | เข้าถึงได้จริง |
| 7 | อัปเดต `NOTES_gotchas.md` + `FEATURES.md` | จดของที่เจอ |
| 8 | push → รอ build ~1 นาที → ทดสอบมือถือจริง + ลองเซฟ PDF | — |

---

## 8. ยากง่าย: **ปานกลาง** · ไม่ต้องมี backend/AI/ค่าใช้จ่าย

- ง่าย: รวม/เรียง/กรองข้อมูล (รู้โครงครบแล้ว) · ภาพรวมหัว (reuse `TC.ageText`) · ปุ่ม preset
- ปานกลาง: layout ตอนพิมพ์ PDF (ซ่อน control, ตัดหน้า) · จับกลุ่มผลเลือดตามวัน
- ประเมิน ~ ครึ่งวัน–วัน

---

## 9. กับดักที่ต้องระวัง (จาก `NOTES_gotchas.md`)

- **แคช** — `timeline.html` เป็น entry point ใหม่ ติด `?v=` ไม่ได้ (เหมือน index/pet/family/login) → แก้แล้วต้อง **hard refresh** · ส่วน `theme.css`/`lib.js` ที่มันเรียก ต้องใส่ `?v=<เลขปัจจุบัน>` และ bump ตามกติกาเดิม
- **โหลดผ่าน `TC.getPet(id)`** — RLS คุมสิทธิ์ให้แล้ว (เห็นเฉพาะน้องที่มีสิทธิ์) · ต้อง `TC.requireAuth()` ก่อน เหมือนหน้าอื่น
- **วันที่** — ข้อมูลเก็บ `YYYY-MM-DD` · เทียบช่วงด้วย string เทียบได้ตรงๆ หรือแปลง `new Date` (ระวัง timezone — ใช้เทียบแบบ date-only เหมือน `nextAppt` เดิม)
- **ผลเลือดหลายหมวด/วัน** — ต้อง group ตาม `date` ก่อน ไม่งั้นได้หลายแถวซ้ำวัน
- **สีป้ายตอนพิมพ์** — เบราว์เซอร์ตัดสีพื้นหลังตอน print โดยดีฟอลต์ ต้อง `-webkit-print-color-adjust: exact; print-color-adjust: exact`
- **รูป** — ใน timeline โชว์แค่ "📷 n รูป" (ไม่ฝังรูป) · รูปเป็น signed URL ต้อง async + หมดอายุ ไม่เหมาะกับ PDF รอบนี้

---

## 10. Test checklist

- [ ] เลือก 7/30/365 วัน แล้วรายการกรองถูกช่วง
- [ ] กำหนดเอง (ปฏิทิน) เลือกวันเริ่ม–สิ้นสุดแล้วกรองถูก
- [ ] เรียงใหม่→เก่าถูกต้อง · วันเดียวกันหลายเหตุการณ์อยู่ครบ
- [ ] ผลเลือดวันเดียวหลายหมวด = แถวเดียว บอกชื่อหมวดครบ ไม่โชว์ค่า
- [ ] น้ำหนัก "จาก→ถึง" คำนวณจากตัวแรก/ตัวล่าสุดในช่วงถูก
- [ ] ช่วงว่าง = "ไม่มีบันทึกในช่วงนี้" ไม่ error
- [ ] กดบันทึก PDF → ได้หน้าสะอาด (ไม่มีปุ่ม/แถบ) หัวมีชื่อน้อง+ช่วงวัน สีป้ายติด
- [ ] vet (ดูอย่างเดียว) เปิดได้ · คนไม่มีสิทธิ์เปิด id นี้ไม่ได้ (RLS)
- [ ] มือถือจริง: ตาราง/ปฏิทินไม่ล้นจอ · print เป็น PDF ได้ทั้ง iOS/Android

---

## 11. อยู่นอกขอบเขตรอบนี้ (backlog)

- ฝังรูปจริงลง PDF
- โชว์ค่าผลเลือดในไทม์ไลน์ (ตอนนี้ลิงก์ไปแท็บผลเลือดแทน)
- กรองตามประเภท (เช่น ดูเฉพาะการรักษา) / ค้นหา
- ส่งออก CSV / แชร์ลิงก์
- **ไม่ยุบรวมกับ "ปฏิทินรวม"** — คนละจุดประสงค์: Timeline = รายงานเชิงเส้นไว้ export/พิมพ์เอาไปหาหมอ (สรุปส่งต่อ) · ปฏิทินรวม = มุมมอง grid ในแอปไว้ดูเร็วๆ ประจำวัน (ใช้งานประจำ) · ทำแยกกัน
