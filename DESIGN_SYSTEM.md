# Tammie Care — Design System (พาสเทลสว่าง)

ธีมเว็บดูแลสุขภาพสัตว์เลี้ยง โทน **น่ารัก อบอุ่น พาสเทลสว่าง** ดึงสีจากแบนเนอร์หัวเว็บ
(หมาโกลเด้นใส่เสื้อกาวน์หมอ พื้นครีม ตัวอักษร "Tammie Care" ลายมือสีฟ้าเดนิม ลายอุ้งเท้า/หัวใจ/ใบไม้พาสเทล)

แนวทาง: สว่าง สะอาด มน ๆ ฟอนต์กลมเป็นมิตร ใช้สีตัดเฉพาะจุดสำคัญ (เตือน/อันตราย) คอนทราสต์อ่านง่าย

---

## 1. สี (Color tokens)

### พื้นหลัง / พื้นผิว
| Token | Hex | ใช้กับ |
|---|---|---|
| `--bg` | `#fdf4eb` | พื้นหลังหน้าหลัก (ครีม) |
| `--bg2` | `#f9ecdd` | พื้นครีมเข้มขึ้นเล็กน้อย |
| `--surface` | `#ffffff` | การ์ด / กล่องหลัก |
| `--surface2` | `#fbf4ec` | แผงรอง / หัวตาราง / ปุ่มเล็ก |
| `--border` | `#efe0cd` | เส้นขอบโทนอุ่น (ค่าเริ่มต้น) |
| `--border-blue` | `#d8e2f0` | เส้นขอบโทนฟ้า (กรอบแบนเนอร์) |

### สีหลัก / แอ็กเซนต์
| Token | Hex | ใช้กับ |
|---|---|---|
| `--blue` | `#4a6fa5` | สีหลัก = ฟ้าเดนิมจากตัวอักษรแบนเนอร์ (หัวข้อ/ลิงก์เน้น) |
| `--sky` | `#5b86bf` | ฟ้ารอง (ลิงก์ ไอคอน ชื่อเล่น) |
| `--sky-soft` | `#a9c4e8` | ฟ้าหวานอ่อน (แถบแอ็กเซนต์ ขอบรูป) |
| `--sky-dim` | `#6f97cf` | ฟ้ากลาง (พื้นแท็บ active + ตัวอักษรขาว) |
| `--gold` | `#d99e3e` | ทอง (ลายอุ้งเท้า ไอคอน เน้นเล็ก) |
| `--gold-dim` | `#e3c485` | ทองอ่อน (เส้นขอบ/พื้นแต้ม) |
| `--pink` | `#e69bbf` | ชมพูพาสเทล (แอ็กเซนต์คู่ฟ้า) |
| `--pink-dim` | `#dd92bb` | ชมพูกลาง (ไล่เฉดกับฟ้า) |

### สีสถานะ (semantic)
| Token | Hex | ความหมาย |
|---|---|---|
| `--green` | `#5fa57f` | ปกติ / ดี |
| `--green-dim` | `#ddf0e6` | พื้นแต้มเขียวอ่อน |
| `--red` | `#d96a6a` | เตือน / อันตราย |
| `--red-dim` | `#f8e0e0` | พื้นแต้มแดงอ่อน (กล่องอันตราย) |

> สีไฟ Leuco Plus: 🔴 `<4000` / 🟡 `4000–4499` / 🟢 `≥4500`

### ตัวอักษร
| Token | Hex | ใช้กับ |
|---|---|---|
| `--text` | `#4a4035` | ตัวอักษรหลัก (น้ำตาลเข้มอุ่น อ่านง่ายบนครีม) |
| `--text-dim` | `#8c7f6f` | ตัวอักษรรอง |
| `--text-muted` | `#b6a795` | ตัวอักษรจาง / placeholder |

### สีตัวอักษรเน้นบนพื้นแต้ม (เพื่อคอนทราสต์)
- บนกล่องอันตราย (พื้น `--red-dim`): หัวข้อ `#b83d3d`, รายการ `#8a2b2b`, จุด bullet `#c0392b`
- ป้ายแดง: `#c44d4d` · ป้ายทอง/เหลือง: `#936e1c` · ป้ายเขียว: `#34795a`
- ตัวอักษรบนปุ่ม/ป้ายสีสด (gradient): `#1a1320` (น้ำตาลเข้มเกือบดำ) หรือ `#ffffff`

---

## 2. ฟอนต์ (Typography)

โหลดจาก Google Fonts: `Sarabun`, `Mali`, `IBM Plex Mono`

| บทบาท | ฟอนต์ | น้ำหนัก | ใช้กับ |
|---|---|---|---|
| Display / น่ารัก | **Mali** | 400–700 | หัวข้อ, ชื่อสัตว์, label หมวด, ปุ่ม, footer |
| Body | **Sarabun** | 300–700 | เนื้อหาทั่วไป, ฟอร์ม, ตาราง |
| Mono | **IBM Plex Mono** | 400–500 | วันที่, label เล็กตัวพิมพ์ใหญ่ (uppercase + letter-spacing) |

ขนาดอ้างอิง: body 15px · หัวข้อหน้า 30px · ชื่อการ์ด 21–22px · label หมวด 15–17px · ป้าย/hint 11–13px
line-height เนื้อหา 1.6–1.8

---

## 3. รัศมีขอบ (Border radius)

| ค่า | ใช้กับ |
|---|---|
| `24px` | กรอบแบนเนอร์หัวเว็บ |
| `20–22px` | การ์ดสัตว์เลี้ยง, การ์ดทั่วไป, ปุ่มหลัก |
| `20px` | ป้าย/pill (badge, สถานะ, แจ้งเตือน) |
| `10–14px` | กล่องย่อย (alert, leuco, med-box) |
| `8px` | input, select, textarea, การ์ดรายการ, ปุ่มเล็ก |
| `5–6px` | ช่องปฏิทิน, tooltip, ปุ่มนำทาง |
| `50%` | รูปโปรไฟล์ / avatar (วงกลม) |

---

## 4. เงา (Shadow)

โทนเงาเป็น **น้ำตาลอุ่น/ฟ้าจาง** (ไม่ใช้ดำล้วน) ให้ฟุ้งนุ่ม

| ใช้กับ | ค่า |
|---|---|
| การ์ด (ปกติ) | `0 4px 16px rgba(160,140,110,0.10)` |
| การ์ด (hover) | `0 16px 32px rgba(91,134,191,0.20)` ยก `translateY(-5px)` |
| แบนเนอร์หัวเว็บ | `0 12px 34px rgba(91,134,191,0.18), 0 3px 10px rgba(217,158,62,0.10)` |
| header sticky | `0 2px 14px rgba(160,140,110,0.16)` |
| กล่องรายละเอียด | `0 6px 18px rgba(160,140,110,0.18)` |
| รูปโปรไฟล์ | `0 4px 12px rgba(91,134,191,0.18)` |
| tooltip | พื้น `#4a4035` ตัวอักษรขาว เงา `0 6px 16px rgba(74,64,53,0.28)` |

---

## 5. องค์ประกอบตกแต่ง (Decorative / สร้างความน่ารัก)

- **พื้นหลังลายอุ้งเท้า**: SVG tile (140×140) สีทอง `#c79a55` opacity `0.06` ปูทั้งหน้า, `background-attachment: fixed`
- **แสงไล่เฉด (glow)** บนพื้นหลัง:
  - มุมบนซ้าย: `radial-gradient(circle at 12% 6%, rgba(169,196,232,0.16), transparent 36%)` (ฟ้า)
  - มุมล่างขวา: `radial-gradient(circle at 88% 96%, rgba(230,155,191,0.10), transparent 40%)` (ชมพู)
- **แถบแอ็กเซนต์บนการ์ด** (เส้นบนสุดหนา 5px): `linear-gradient(90deg, var(--sky-soft), var(--pink), var(--gold-dim))`
- **วงแหวนรูปโปรไฟล์/avatar**: `linear-gradient(135deg, var(--sky-soft), var(--pink))` เป็นขอบ 3px
- **ปุ่มหลัก**: `linear-gradient(135deg, var(--sky), var(--pink))` ตัวอักษร `#1a1320`
- **อนิเมชัน**: กระดิ่งแจ้งเตือน 🔔 สั่นเป็นจังหวะ (`bell-wiggle`), การ์ด hover ยกขึ้นเล็กน้อย
- **อิโมจิ**: 🐾 💙 💊 🧪 📅 🚨 🔔 ใช้ประกอบหัวข้อ/ป้ายให้เป็นมิตร

---

## 6. รูปแบบป้าย/สถานะ (Pill pattern)

ป้ายทรงแคปซูล (radius 20px): พื้นสีจาง (tint ~12–16% ของสีหลัก) + เส้นขอบสีเดียวกันเข้มขึ้น + ตัวอักษรสีเข้ม
- เฝ้าระวัง = ทอง · ปกติ = เขียว · ต้องดูแล = แดง
- แจ้งเตือนนัด: กรอบทอง = ภายใน 7 วัน, กรอบแดง = วันนี้/พรุ่งนี้

---

## 7. แท็บหลัก (Tab navigation)

หน้า dashboard (pet.html) และ admin ใช้ระบบแท็บหลักหน้าตาเดียวกัน (`.dash-tabs` + `.dtab` + panel)
- แท็บปกติ: pill พื้นขาว ขอบ `--border` ตัวอักษร `--text-dim`
- แท็บ active: `linear-gradient(135deg, var(--sky), var(--pink))` ตัวอักษร `#1a1320` เงา `0 4px 14px rgba(143,189,240,0.3)` ไม่มีขอบ
- แถบแท็บมีเส้นใต้ `1px solid var(--border)` คั่นจากเนื้อหา
- ลำดับแท็บ dashboard: **สรุป / อาการประจำวัน / การรักษา / ผลเลือด / 💊 ยา / 📅 นัดหมาย** · admin ลำดับเดียวกัน + ปิดท้ายด้วย **⚙️ ตั้งค่า** (ตั้งค่าหน้า Dashboard)
- **inline list pattern** (admin): ทุกหมวดมีลิสต์รายการเดิมด้านบนฟอร์ม ใช้ `.token-box` แต่ละแถวมีลิงก์ **แก้** (สีทอง `--gold`) · **ลบ** (สีแดง `--red`) — กดแก้ดึงขึ้นฟอร์ม ปุ่มหลักเปลี่ยน label เป็น "บันทึกการแก้ไข"
- **checkbox config rows** (admin ตั้งค่า): ใช้ `.disp-row` (แถวขอบมน พื้น `--surface2`) + `input[type=checkbox]` (accent `--gold`) — ใช้ทั้งตั้งค่าหน้า Dashboard และเลือกอาการประจำวันต่อโปรไฟล์

---

## 8. Flowchart (Mermaid)

ใช้ Mermaid (cdnjs 10.9.1) เรนเดอร์ flowchart ในผลการรักษา · init ด้วย `theme:'base'` + themeVariables โทนพาสเทล:
- `primaryColor: #fbf4ec` · `primaryBorderColor: #d99e3e` · `primaryTextColor: #4a4035`
- `lineColor: #8c7f6f` · `secondaryColor: #e6eef9` · `tertiaryColor: #f8e0e0`
- `fontFamily: 'Sarabun, sans-serif'` · `fontSize: 14px`

กล่องครอบ flowchart (`.tx-flow`): พื้นขาว ขอบ `--gold-dim` radius 8px padding 12px · มี label เล็ก "🗺 ขั้นตอน/แผนการรักษา" (IBM Plex Mono สีทอง) · `svg { max-width:100%; height:auto }`

---

## 9. ข้อความหลายบรรทัด

ช่อง free-text จาก admin (textarea) เก็บ `\n` ได้ · ฝั่งแสดงใช้ `white-space: pre-line` กับ
`.alert-box ul`, `.watch-box ul`, `.tl-item p`, `.nb-detail-card p`, `.appt-note`, `.med-note` เพื่อให้ขึ้นบรรทัดใหม่ตามที่พิมพ์

---

## โทนและน้ำเสียง (Voice)

ภาษาไทยเป็นกันเอง อบอุ่น ห่วงใย ("ไม่ได้แค่เลี้ยง แต่เราแคร์") เหมาะกับเจ้าของสัตว์ที่กำลังดูแลสัตว์ป่วย —
ให้ความรู้สึกปลอดภัย อ่อนโยน แต่ข้อมูลสุขภาพต้องชัดและอ่านง่าย โดยเฉพาะส่วนเตือน/อันตราย

---

## หมายเหตุการนำไปใช้
- เว็บเป็น **static HTML/JS** ล้วน + ข้อมูลจาก `data/*.json` — design system นี้ครอบ "หน้าตา" ส่วนตรรกะดึงข้อมูลคงเดิม
- ตัวแปรทั้งหมดประกาศใน `:root` ของแต่ละไฟล์ (`index.html` / `pet.html` / `admin.html`) เปลี่ยนที่เดียวกระทบทั้งหน้า · ทั้ง 3 หน้าใช้ palette เดียวกัน (ธีมพาสเทลสว่าง)
- แบนเนอร์อ้างอิง: `img/header.jpg`
