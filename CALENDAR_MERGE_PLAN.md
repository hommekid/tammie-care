# 🗓️ แผน: รวมปฏิทิน (อาการประจำวัน + การรักษา → ปฏิทินเดียว)

> **สถานะ: ✅ ทำแล้ว (13 ส.ค. 2026, `?v=39`)** · branch `v2-multiuser` · ดูสรุปที่ `NOTES_gotchas.md` กับดักข้อ 14
> ต่างจาก `TIMELINE_PLAN.md` — อันนั้นคือรายงาน export ไปหาหมอ · อันนี้คือมุมมองในแอปไว้ดูประจำวัน

## เป้าหมาย
แท็บปฏิทินเดียว "🗓️ ปฏิทิน" **แทน** แท็บ "อาการประจำวัน" + "การรักษา" · **นัดหมายแยกไว้** (อดีต vs อนาคต)

## การตัดสินใจที่เคาะแล้ว
| เรื่อง | เคาะ |
|---|---|
| ขอบเขต | แทน อาการ+การรักษา ด้วยปฏิทินเดียว · นัดหมายคงไว้ |
| "หาหมอ" (การรักษา) | **หมวดพิเศษ คงฟอร์มการรักษาเดิม** (หมอ/แผนก/flowchart/รูป) · recolor ได้ · **ลบไม่ได้** |
| หมวดอาการ | เก็บของเดิม (ทั่วไป/ท้องเสีย/เลือดกำเดา) เป็นหมวดเหมือนที่ user เพิ่มเอง |
| เปลี่ยนสีจุด | ได้ทุกหมวด (รวมหาหมอ) |
| เพิ่มหมวด | ได้ (หมวดอาการใหม่) |
| ลบหมวด | ได้ (ยกเว้น อาการทั่วไป + หาหมอ) · **เนื้อหาย้ายไป "อาการทั่วไป" อัตโนมัติ ไม่ลบทิ้ง** + เตือนก่อน |

## โครงข้อมูล (ไม่ยุบรวม — presentation merge)
- อาการยังอยู่ `symptoms[หมวด][]` · การรักษายังอยู่ `treatments[]` เหมือนเดิม (ฟอร์มแยก)
- สีหมวดอาการ: `display.tabs=[{source,label,color}]` (เดิม) · **สีหาหมอ: `display.treatColor`** (ฟิลด์ใหม่ แยกไว้ ไม่ยุ่ง treatments data)
- ปฏิทินรวม event = อาการทุกหมวด + การรักษา · dot สีตามหมวด (`symColor('treat')` = treatColor)

## ขั้นตอน
1. `TABS`: เอา `daily`+`treat` ออก ใส่ `{key:'calendar', label:'🗓️ ปฏิทิน'}` · แก้ dispatch ใน `showTab`
2. `renderCalendar()` ใหม่ — รวม events อาการ+การรักษา · legend ทุกหมวด+หาหมอ · ปุ่ม "➕ เพิ่มบันทึก" เลือกหมวด → หาหมอ=ฟอร์มการรักษา, อื่น=ฟอร์มอาการ · `onDetail` โชว์ทั้ง sym-card + tx-entry ของวันนั้น (edit/del ต่อสายเดิม)
3. `toggleCatCfg()` ขยาย — เพิ่มแถวหาหมอ (recolor only) · ลบหมวด = ย้ายเข้า general + เตือน (กัน general/treat)
4. เก็บ `renderDaily`/`renderTreat` helper (openSymForm/saveSymptom/delSymptom/openTreatForm/delTreat) ไว้ reuse
5. bump `?v=` · อัปเดต NOTES/FEATURES/PLAN

## กับดัก
- entry point เดิม `?v=` ไม่ได้ · bump theme/lib
- `nosebleed` มี field `side` — ย้ายเข้า general แล้ว general ไม่โชว์ side (ไม่พัง ข้อมูลค้างไว้เฉยๆ)
- ลบ "อาการทั่วไป" (general) เองไม่ได้ (เป็นปลายทาง fallback) · ลบ "หาหมอ" ไม่ได้
- คน migrate: เฟ่/เฟ่อ display.tabs เดิมยังใช้ได้ · treatColor ไม่มี = default ทอง `#d99e3e`
